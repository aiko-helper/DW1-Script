// source: extract/DIGIMON/SCN/MAPHEAD.SCN
// MAPHEAD.SCN, section 1244

section section_1244(1244) {  // len=110
    playSound(0, 7);
    delay(10);
    setDialogOwner(Entity.Player);
    if (pstat[254] != 0) goto L_0032;
    showTextbox("It's <LIGHTBLUE><SOME ITEM><WHITE>!\n");
    goto L_006c;
L_0032:
    showTextbox("But we got too many Items.\n");
L_006c:
    return;
}
