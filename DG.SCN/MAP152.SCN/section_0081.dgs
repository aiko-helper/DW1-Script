// source: extract/DIGIMON/SCN/DG.SCN
// MAP152.SCN, section 81

section section_81(81) {  // len=66
    rotate3DObject(1, 1);
    playSound(8, 2);
    delay(20);
    move(entity: Entity.Player, to: (-1999, 800), sprint: 0);
    if (!trigger(329)) goto L_003c;
    if (!trigger(209) && !trigger(208) && !trigger(222) && !trigger(223)) goto L_003c;
    warp(to: Map.FACT08A, spawn: 0, trigger: 255);
L_003c:
    warp(to: Map.FACT08B, spawn: 0, trigger: 255);
    return;
}
