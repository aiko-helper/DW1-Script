// DG218.SCN
#include "functions.dgs"
#pragma script index=218 start=0xbd800 end=0xbf800 headerU16=0xc sentinelOff=0xfe
#include "entities.dgs"

section onEnter(254) {
  return;
}

section(51) {
  if (trigger(223) && pstat[27] == 3) {
    speak(Entity(5), "What will you eat?\n");
    goto L1;
  }
  if (trigger(228) && pstat[27] == 4) {
    speak(Entity(5), "What do you want to eat?\n");
  } else {
    if (!trigger(256) || pstat[27] != 5) goto L1;
    speak(Entity(5), "......?\n");
    goto L14;

L1:
    {
      setDialogOwner(Entity.System);
      choice { "Snow Shake: three hund bits" -> L2, "Ice Cream:  seven hund bits" -> L4, "Snow Cone:sixteen hund bits" -> L6 }
      break;

L2:
      if (money >= 300) {
      } else {
        speak(Entity(5), "You don't have enough bits.\n");
        return;
      }
      if (trigger(640) && (cond & 0x04) == 0x00) goto L3;
      speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed\na Snow Shake!\n");
      addStats(offense: 0, defense: 0, speed: 1, brains: 0, maxHP: 0, maxMP: 10);
      reduceStats(tiredness: 2);
      addStats(happiness: 3, discipline: 0, energy: 12, weight: 2);
      reduceMoney(300);
      speak(Entity.System, "MP went up ten!\nSpeed went up one!\n");
      showTextbox("Happiness went up!\nTiredness went down!\n");
      playAnimation(Entity.Partner, Animation(11));
      waitForEntity(Entity.Partner);
      break;

L3:
      playAnimation(Entity.Partner, Animation(13));
      waitForEntity(Entity.System);
      speak(Entity(5), "<ORANGE><DIGIMON><WHITE> isn't hungry.\n");
      return;

L4:
      if (money >= 700) {
      } else {
        speak(Entity(5), "You don't have enough bits.\n");
        return;
      }
      if (trigger(640) && (cond & 0x04) == 0x00) goto L5;
      speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed\nan Ice Cream!\n");
      addStats(offense: 0, defense: 0, speed: 2, brains: 0, maxHP: 0, maxMP: 30);
      reduceStats(tiredness: 2);
      addStats(happiness: 5, discipline: 0, energy: 16, weight: 3);
      reduceMoney(700);
      speak(Entity.System, "MP went up thirty!\nSpeed went up two!\n");
      showTextbox("Happiness went up!\nTiredness went down!\n");
      playAnimation(Entity.Partner, Animation(11));
      waitForEntity(Entity.Partner);
      break;

L5:
      playAnimation(Entity.Partner, Animation(13));
      waitForEntity(Entity.System);
      speak(Entity(5), "<ORANGE><DIGIMON><WHITE> isn't hungry.\n");
      return;

L6:
      if (money >= 1600) {
      } else {
        speak(Entity(5), "You don't have enough bits.\n");
        return;
      }
      if (trigger(640) && (cond & 0x04) == 0x00) goto L7;
      speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed\na Snow Cone!\n");
      addStats(offense: 0, defense: 0, speed: 2, brains: 0, maxHP: 0, maxMP: 50);
      reduceStats(tiredness: 2);
      addStats(happiness: 7, discipline: 1, energy: 20, weight: 4);
      reduceMoney(1600);
      speak(Entity.System, "MP went up fifty!\nSpeed went up two!\n");
      showTextbox("Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
      playAnimation(Entity.Partner, Animation(11));
      waitForEntity(Entity.Partner);
      break;

L7:
      playAnimation(Entity.Partner, Animation(13));
      waitForEntity(Entity.System);
      speak(Entity(5), "<ORANGE><DIGIMON><WHITE> isn't hungry.\n");
      return;
    }
    speak(Entity(5), "See you later!\n");
    return;
  }
  {
    setDialogOwner(Entity.System);
    choice { "Space Salad:fifteen hund bit" -> L8, "UFO Soup:     four thous bit" -> L10, "Galaxy Course:nine thous bit" -> L12 }
    break;

L8:
    if (money >= 1500) {
    } else {
      speak(Entity(5), "You're short of bits.\n");
      return;
    }
    if (trigger(640) && (cond & 0x04) == 0x00) goto L9;
    speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed\na Space Salad!\n");
    addStats(offense: 1, defense: 1, speed: 1, brains: 1, maxHP: 10, maxMP: 10);
    reduceStats(tiredness: 5);
    addStats(happiness: 1, discipline: 1, energy: 20, weight: 0);
    reduceMoney(1500);
    speak(Entity.System, "HP went up ten!\nMP went up ten!\nOffensive Power went up one!\n");
    showTextbox("Defensive Power went up one!\nSpeed went up one!\nBrains went up one!\n");
    showTextbox("Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
    playAnimation(Entity.Partner, Animation(11));
    waitForEntity(Entity.Partner);
    break;

L9:
    playAnimation(Entity.Partner, Animation(13));
    waitForEntity(Entity.System);
    speak(Entity(5), "<ORANGE><DIGIMON><WHITE> is full.\n");
    return;

L10:
    if (money >= 4000) {
    } else {
      speak(Entity(5), "You're short of bits.\n");
      return;
    }
    if (trigger(640) && (cond & 0x04) == 0x00) goto L11;
    speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed\na UFO Soup!\n");
    addStats(offense: 2, defense: 2, speed: 2, brains: 2, maxHP: 20, maxMP: 20);
    reduceStats(tiredness: 5);
    addStats(happiness: 2, discipline: 2, energy: 30, weight: 1);
    reduceMoney(4000);
    speak(Entity.System, "HP went up twenty!\nMP went up twenty!\nOffensive Power went up two!\n");
    showTextbox("Defensive Power went up two!\nSpeed went up two!\nBrains went up two!\n");
    showTextbox("Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
    playAnimation(Entity.Partner, Animation(11));
    waitForEntity(Entity.Partner);
    break;

L11:
    playAnimation(Entity.Partner, Animation(13));
    waitForEntity(Entity.System);
    speak(Entity(5), "<ORANGE><DIGIMON><WHITE> is full.\n");
    return;

L12:
    if (money >= 9000) {
    } else {
      speak(Entity(5), "You're short of bits.\n");
      return;
    }
    if (trigger(640) && (cond & 0x04) == 0x00) goto L13;
    speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed\na Galaxy Course!\n");
    addStats(offense: 3, defense: 3, speed: 3, brains: 3, maxHP: 30, maxMP: 30);
    reduceStats(tiredness: 5);
    addStats(happiness: 3, discipline: 3, energy: 45, weight: 5);
    reduceMoney(9000);
    speak(Entity.System, "HP went up thirty!\nMP went up thirty!\nOffense Power went up three!\n");
    showTextbox("Defense Power went up three!\nSpeed went up three!\nBrains went up three!\n");
    showTextbox("Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
    playAnimation(Entity.Partner, Animation(11));
    waitForEntity(Entity.Partner);
    break;

L13:
    playAnimation(Entity.Partner, Animation(13));
    waitForEntity(Entity.System);
    speak(Entity(5), "<ORANGE><DIGIMON><WHITE> is full.\n");
    return;
  }
  speak(Entity(5), "See you guys later.\n");
  return;

L14:
  {
    setDialogOwner(Entity.System);
    choice { "Boiled Egg:   one thous bits" -> L15, "Omelet:twenty five hund bits" -> L17, "Egg Bowl:     six thous bits" -> L19 }
    break;

L15:
    if (money >= 1000) {
    } else {
      speak(Entity(5), ".......\n");
      return;
    }
    if (trigger(640) && (cond & 0x04) == 0x00) goto L16;
    speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed a\nWonder Boiled Egg!\n");
    roll(pstat: PStat.RollScratch, max: 2);

L16:
    if (pstat[110] <= 0) {
      addStats(offense: 1, defense: 0, speed: 1, brains: 0, maxHP: 10, maxMP: 0);
      reduceStats(tiredness: 5);
      addStats(happiness: 1, discipline: 0, energy: 18, weight: 1);
      reduceMoney(1000);
      speak(Entity.System, "HP went up ten!\nOffensive Power went up one!\nSpeed went up one!\n");
      speak(Entity.System, "Happiness went up!\nTiredness went down!\n");
      break;
    }
    if (pstat[110] <= 1) {
      addStats(offense: 0, defense: 1, speed: 0, brains: 1, maxHP: 0, maxMP: 10);
      reduceStats(tiredness: 5);
      addStats(happiness: 1, discipline: 0, energy: 18, weight: 1);
      reduceMoney(1000);
      speak(Entity.System, "MP went up ten!\nDefensive Power went up one!\nBrains went up one!\n");
      speak(Entity.System, "Happiness went up!\nTiredness went down!\n");
      break;
    }
    if (pstat[110] <= 2) {
      addStats(offense: 2, defense: 2, speed: 2, brains: 2, maxHP: 20, maxMP: 20);
      reduceStats(tiredness: 5);
      addStats(happiness: 2, discipline: 1, energy: 18, weight: 1);
      reduceMoney(1000);
      speak(Entity.System, "HP went up twenty!\nMP went up twenty!\nOffensive Power went up two!\n");
      showTextbox("Defensive Power went up two!\nSpeed went up two!\nBrains went up two!\n");
      speak(Entity.System, "Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
      break;
    }
    playAnimation(Entity.Partner, Animation(11));
    waitForEntity(Entity.Partner);
    break;
    playAnimation(Entity.Partner, Animation(13));
    waitForEntity(Entity.System);
    speak(Entity(5), ".......!\n");
    return;

L17:
    if (money >= 2500) {
    } else {
      speak(Entity(5), "..........\n");
      return;
    }
    if (trigger(640) && (cond & 0x04) == 0x00) goto L18;
    speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed a\nWonder Egg Omelet!\n");
    roll(pstat: PStat.RollScratch, max: 2);

L18:
    if (pstat[110] <= 0) {
      addStats(offense: 2, defense: 0, speed: 2, brains: 0, maxHP: 20, maxMP: 0);
      reduceStats(tiredness: 5);
      addStats(happiness: 2, discipline: 0, energy: 24, weight: 2);
      reduceMoney(2500);
      speak(Entity.System, "HP went up twenty!\nOffensive Power went up two!\nSpeed went up two!\n");
      speak(Entity.System, "Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
      break;
    }
    if (pstat[110] <= 1) {
      addStats(offense: 0, defense: 2, speed: 0, brains: 2, maxHP: 0, maxMP: 20);
      reduceStats(tiredness: 5);
      addStats(happiness: 2, discipline: 0, energy: 24, weight: 2);
      reduceMoney(2500);
      speak(Entity.System, "MP went up twenty!\nDefensive Power went up two!\nBrains went up two!\n");
      speak(Entity.System, "Happiness went up!\nTiredness went down!\n");
      break;
    }
    if (pstat[110] <= 2) {
      addStats(offense: 3, defense: 3, speed: 3, brains: 3, maxHP: 30, maxMP: 30);
      reduceStats(tiredness: 5);
      addStats(happiness: 3, discipline: 1, energy: 24, weight: 2);
      reduceMoney(2500);
      speak(Entity.System, "HP went up thirty!\nMP went up thirty!\nOffense Power went up three!\n");
      showTextbox("Defense Power went up three!\nSpeed went up three!\nBrains went up three!\n");
      speak(Entity.System, "Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
      break;
    }
    playAnimation(Entity.Partner, Animation(11));
    waitForEntity(Entity.Partner);
    break;
    playAnimation(Entity.Partner, Animation(13));
    waitForEntity(Entity.System);
    speak(Entity(5), ".......!\n");
    return;

L19:
    if (money >= 6000) {
    } else {
      speak(Entity(5), "..........\n");
      return;
    }
    if (trigger(640) && (cond & 0x04) == 0x00) goto L20;
    speak(Entity.System, "<ORANGE><DIGIMON><WHITE> enjoyed a\nWonder Egg Bowl!\n");
    roll(pstat: PStat.RollScratch, max: 2);

L20:
    if (pstat[110] <= 0) {
      addStats(offense: 3, defense: 0, speed: 3, brains: 0, maxHP: 30, maxMP: 0);
      reduceStats(tiredness: 5);
      addStats(happiness: 3, discipline: 1, energy: 36, weight: 3);
      reduceMoney(6000);
      speak(Entity.System, "HP went up thirty!\nOffense Power went up thirty!\nSpeed went up thirty!\n");
      speak(Entity.System, "Happiness went up!\nDiscipline went up!\nTiredness went down!\n");
      break;
    }
    if (pstat[110] <= 1) {
      addStats(offense: 0, defense: 3, speed: 0, brains: 3, maxHP: 0, maxMP: 30);
      reduceStats(tiredness: 5);
      addStats(happiness: 3, discipline: 1, energy: 36, weight: 3);
      reduceMoney(6000);
      speak(Entity.System, "MP went up thirty!\nDefense Power went up three!\nBrains went up three!\n");
      speak(Entity.System, "Happiness went up!\nDiscipline went up!\nTiredness has went up!\n");
      break;
    }
    if (pstat[110] <= 2) {
      addStats(offense: 4, defense: 4, speed: 4, brains: 4, maxHP: 40, maxMP: 40);
      reduceStats(tiredness: 5);
      addStats(happiness: 4, discipline: 3, energy: 36, weight: 3);
      reduceMoney(6000);
      speak(Entity.System, "HP went up forty!\nMP went up forty!\nOffense Power went up four!\n");
      showTextbox("Defense Power went up four!\nSpeed went up four!\nBrains went up four!\n");
      speak(Entity.System, "Happiness went up!\nDiscipline went up!\nTiredness went down.\n");
      break;
    }
    playAnimation(Entity.Partner, Animation(11));
    waitForEntity(Entity.Partner);
    break;
    playAnimation(Entity.Partner, Animation(13));
    waitForEntity(Entity.System);
    speak(Entity(5), "........!\n");
    return;
  }
  speak(Entity(5), "..........\n");
  return;
  garbage { 0c 00 fe 00 0c 00 33 00 0e 00 ff ff fe 00 19 00 00 00 df 00 88 00 1b 03 18 00 4e 00 19 00 1b 05 1a 00 82 76 82 88 82 81 82 94 81 40 82 97 82 89 82 8c 82 8c 81 40 82 99 82 8f 82 95 81 40 82 85 82 81 82 94 81 48 0d 00 00 00 16 00 c4 00 19 00 00 00 e4 00 88 00 1b 04 18 00 9a 00 19 00 1b 05 1a 00 82 76 82 88 82 81 82 94 81 40 82 84 82 8f 81 40 82 99 82 8f 82 95 81 40 82 97 82 81 82 8e 82 94 81 40 82 94 82 8f 81 40 82 85 82 81 82 94 81 48 0d 00 00 00 16 00 da 06 19 00 00 00 00 01 88 00 1b 05 18 00 c4 00 19 00 1b 05 1a 00 }
}
