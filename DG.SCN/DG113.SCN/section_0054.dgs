// source: extract/DIGIMON/SCN/DG.SCN
// DG113.SCN, section 54

section section_54(54) {  // len=174
    playAnimation(Entity.Player, 24);
    delay(20);
    setDialogOwner(Entity.Player);
    showTextbox("Hey, a bird's feathers are\nfallen here. They are still\nfresh. Are we too late?\n");
    return;
}
